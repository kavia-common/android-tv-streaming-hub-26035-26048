<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- PUBLIC_INTERFACE -->
    <!-- Theme.OceanTV
         Minimal app theme used by the AndroidManifest. Actual UI is Compose Material3.
         Use a Material Components parent that does not depend on old support libs,
         and provides a NoActionBar window for legacy views. -->
    <style name="Theme.OceanTV" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Pre-compose window background -->
        <item name="android:windowBackground">@color/ocean_background</item>
    </style>
</resources>

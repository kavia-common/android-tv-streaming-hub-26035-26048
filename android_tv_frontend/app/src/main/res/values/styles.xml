<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Placeholder theme referenced by Manifest; actual theming handled in Compose.
         Select a base theme guaranteed to exist in the platform without extra libs. -->
    <style name="Theme.OceanTV" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/ocean_background</item>
    </style>
</resources>

<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Placeholder theme name referenced by Manifest; actual theming handled in Compose.
         Use a widely available parent to avoid resource linking failures in CI. -->
    <style name="Theme.OceanTV" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/ocean_background</item>
    </style>
</resources>

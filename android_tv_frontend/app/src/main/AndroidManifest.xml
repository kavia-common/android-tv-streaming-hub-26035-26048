<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Android TV -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <!-- Touchscreen not required for TV -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:banner="@drawable/app_banner">

        <!-- Cast framework options -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.example.android_tv_frontend.cast.CastOptionsProvider" />

        <!-- Mini controller theme (optional) -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_META_DATA"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Search activity for global TV search -->
        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <!-- Details activity -->
        <activity
            android:name=".ui.details.DetailsActivity"
            android:exported="false" />

        <!-- Playback activity -->
        <activity
            android:name=".ui.playback.PlaybackActivity"
            android:exported="false"
            android:banner="@drawable/app_banner" />

        <!-- Live TV list (optional separate entry) -->
        <activity
            android:name=".ui.live.LiveTvActivity"
            android:exported="false" />

        <!-- Optional developer preview WebView -->
        <activity
            android:name=".dev.PreviewWebViewActivity"
            android:exported="false" />
    </application>

</manifest>
